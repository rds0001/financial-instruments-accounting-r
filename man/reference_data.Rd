% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{reference_data}
\alias{reference_data}
\alias{get_policy}
\alias{get_parameters}
\alias{get_schema}
\alias{get_formula}
\alias{get_sources}
\alias{list_profiles}
\alias{load_reference_data}
\alias{export_profile}
\alias{select_snapshot}
\alias{validate_dataset}
\title{Packaged Policies, Schemas and Synthetic Reference Profiles}
\usage{
get_policy(policy_id = "EU_FINANCIAL_INSTRUMENTS_2026_V1")

get_parameters(policy_id = "EU_FINANCIAL_INSTRUMENTS_2026_V1")

get_schema(table = NULL)

get_formula(formula_id, rule_set_id = "FIA_RULES_2026_V1")

get_sources()

list_profiles()

load_reference_data(profile_id)

export_profile(profile_id, destination)

select_snapshot(tables, reporting_date, knowledge_time)

validate_dataset(tables, reporting_date = NULL, knowledge_time = NULL)
}
\arguments{
\item{policy_id}{Explicit accounting-policy identifier. Only the packaged
\code{EU_FINANCIAL_INSTRUMENTS_2026_V1} policy is accepted.}

\item{table}{Optional canonical table name. \code{NULL} returns all 19 schemas.}

\item{formula_id}{Formula registry identifier such as \code{"F-ECL-001"}.}

\item{rule_set_id}{Explicit rule-set identifier. Only
\code{FIA_RULES_2026_V1} is packaged.}

\item{profile_id}{One value returned by \code{list_profiles()}.}

\item{destination}{A new directory path. Existing paths are rejected.}

\item{tables}{Named list containing canonical row lists for all 19 tables,
or a subset for \code{select_snapshot()}.}

\item{reporting_date}{ISO date (\code{YYYY-MM-DD}) used for the valid-time cut-off.}

\item{knowledge_time}{ISO timestamp with explicit UTC offset used for the
transaction-time cut-off.}
}
\value{
\code{get_policy()} and \code{get_parameters()} return a named parameter list;
\code{get_schema()} returns one schema or all schemas; \code{get_formula()} returns
one registry row; \code{get_sources()} returns a list of source-metadata rows;
\code{list_profiles()} returns two stable character identifiers;
\code{load_reference_data()}, \code{select_snapshot()} and \code{validate_dataset()}
return named lists of row lists; \code{export_profile()} invisibly returns the
normalized destination path after writing 19 JSON table files and a JSON
manifest.
}
\description{
Access versioned policy parameters, formula metadata, source metadata,
canonical table schemas and two complete synthetic input profiles. Validate
a dataset or select the unique official bitemporal record at explicit
reporting and knowledge cut-offs. All returned objects are independent R
lists and no function performs a network request.
}
\details{
Resources are synthetic and bundled for deterministic offline use.
\code{select_snapshot()} requires exactly one \code{ACTIVE}, official version per
business key whose valid and known intervals include both cut-offs.
\code{validate_dataset()} checks the exact table/column contract, primary keys,
typed numeric/logical fields, supported view, scenario weights, curve keys
and positive FX rates before returning the selected snapshot. It raises a
condition inheriting from \code{fae_validation_error} on rejection.
}
\examples{
list_profiles()
small <- load_reference_data("SMALL_SA_RETAIL_FIA")
length(small$instruments)
get_schema("instruments")$columns[1:3]
get_policy()$past_due_sicr_days
get_parameters()$default_backstop_days
get_formula("F-ECL-001")$name
length(get_sources())
selected <- select_snapshot(
  list(instruments = small$instruments), "2026-12-31",
  "2026-09-02T12:00:00+02:00"
)
stopifnot(length(selected$instruments) == 5L)
validated <- validate_dataset(small)
stopifnot(length(validated) == 19L)
target <- tempfile("fae-profile-")
export_profile("SMALL_SA_RETAIL_FIA", target)
unlink(target, recursive = TRUE)
}
\references{
International Accounting Standards Board (2014), IFRS 9,
paragraphs 5.5.3--5.5.17. Official source index:
\url{https://www.ifrs.org/issued-standards/list-of-standards/ifrs-9-financial-instruments/}.
}
\seealso{
\code{\link[=run_dataset]{run_dataset()}}, \link{core_formulas}
}
