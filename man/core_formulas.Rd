% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formulas.R
\name{core_formulas}
\alias{core_formulas}
\alias{discount_factor}
\alias{effective_interest_rate}
\alias{amortised_cost_step}
\alias{amortisation_schedule}
\alias{marginal_pd_from_cumulative}
\alias{survival_from_pd}
\alias{cumulative_pd_from_conditional}
\alias{component_ecl}
\alias{recovery_present_value}
\alias{cash_shortfall_ecl}
\alias{modification_gain_loss}
\alias{hedge_ineffectiveness}
\alias{ead_profile}
\alias{lgd_from_recoveries}
\alias{provision_matrix_ecl}
\alias{weighted_ecl}
\alias{poci_allowance_change}
\alias{liability_modification_test}
\alias{coverage_ratio}
\alias{benchmark_ecl}
\title{Core Financial-Instrument Accounting Formulas}
\usage{
discount_factor(rate, year_fraction)

effective_interest_rate(initial_net, cashflows, tolerance = 1e-12)

amortised_cost_step(opening, eir, time, cash_received, adjustment = 0)

amortisation_schedule(opening, eir, cashflows)

marginal_pd_from_cumulative(cumulative)

survival_from_pd(marginal)

cumulative_pd_from_conditional(conditional)

component_ecl(marginal_pd, ead, lgd, eir, year)

recovery_present_value(recoveries, eir)

cash_shortfall_ecl(contractual, expected, eir)

modification_gain_loss(gross_before, modified_cashflows, original_eir)

hedge_ineffectiveness(hedging_change, hedged_change)

ead_profile(drawn, undrawn, ccf, prepayment)

lgd_from_recoveries(ead, recoveries, eir, costs = list())

provision_matrix_ecl(exposure, loss_rate)

weighted_ecl(scenario_losses, probabilities)

poci_allowance_change(current_lifetime_ecl, initial_lifetime_ecl)

liability_modification_test(
  original_present_value,
  modified_present_value,
  qualitative_substantial = FALSE,
  tolerance = 0.1
)

coverage_ratio(allowance, gross_exposure)

benchmark_ecl(iterations = 10000L)
}
\arguments{
\item{rate, eir, original_eir}{Annual effective rate as a decimal, strictly greater than
\code{-1} where discounting is performed.}

\item{year_fraction, time, year}{Non-negative time in years.}

\item{initial_net}{Positive initial net carrying amount.}

\item{cashflows, contractual, expected, modified_cashflows, recoveries, costs}{Two-column matrix/data frame or list of \verb{(time, amount)} pairs. Times and
amounts are finite and non-negative.}

\item{tolerance}{Positive numerical tolerance. For
\code{liability_modification_test()}, the policy threshold in \verb{[0, 1]}.}

\item{opening, gross_before, ead, exposure, allowance, gross_exposure}{Monetary
amounts in the call's currency. Required non-negative unless the function
expressly returns a signed result.}

\item{cash_received}{Non-negative receipt in the period.}

\item{adjustment}{Signed period adjustment.}

\item{cumulative}{Non-decreasing cumulative default probabilities in \verb{[0,1]}.}

\item{marginal}{Unconditional marginal default probabilities in \verb{[0,1]}.}

\item{conditional}{Conditional period default probabilities in \verb{[0,1]}.}

\item{marginal_pd, lgd, loss_rate}{Probabilities or rates in \verb{[0,1]}.}

\item{hedging_change, hedged_change}{Signed fair-value changes; gains are
positive.}

\item{drawn, undrawn}{Non-negative exposure vectors.}

\item{ccf, prepayment}{Equal-length rate vectors in \verb{[0,1]}.}

\item{scenario_losses}{Named non-negative scenario losses.}

\item{probabilities}{Named scenario probabilities whose keys match losses
and whose sum is one within \code{1e-9}.}

\item{current_lifetime_ecl, initial_lifetime_ecl}{Non-negative lifetime loss
estimates; the returned POCI movement remains signed.}

\item{original_present_value, modified_present_value}{Non-negative present
values measured using the original effective interest rate.}

\item{qualitative_substantial}{One non-missing logical management judgement.}

\item{iterations}{Integer count from 1 through 1,000,000.}
}
\value{
Scalar formula functions return one numeric value. Probability and
EAD curve functions return numeric vectors in input order.
\code{amortisation_schedule()} returns a data frame with \code{time}, \code{opening},
\code{interest}, \code{cash_received}, and \code{closing}. The liability modification
test returns a named list with \code{ratio}, \code{substantial}, and \code{threshold}.
\code{coverage_ratio()} returns \code{NA_real_} for a zero denominator. The benchmark
returns iterations, checksum, elapsed seconds, peak bytes (unavailable and
therefore \code{NA_real_} in portable R), and formula identifier.
}
\description{
Pure deterministic calculations for discounting, effective interest,
amortised cost, probability curves, credit loss, recoveries, modifications,
hedges and coverage. Rates and probabilities are decimals; time is measured
in years; monetary values within one call use one currency.
}
\details{
No function rounds monetary results. Inputs must be finite and
missing values are rejected. \code{effective_interest_rate()} uses a bracketed
bisection root over \verb{[-0.999999, 10]}, with at most 250 iterations.
\code{component_ecl()} discounts PD times EAD times LGD to the supplied default
time. Scenario losses must be calculated separately before
\code{weighted_ecl()} is called. Policy judgements, model calibration and data
approval remain caller responsibilities.
}
\examples{
discount_factor(0.05, 1)
stopifnot(abs(effective_interest_rate(1000, list(c(1, 1100))) - 0.1) < 1e-10)
amortised_cost_step(1000, 0.1, 1, 200)
amortisation_schedule(1000, 0.1, list(c(1, 200), c(2, 990)))
marginal_pd_from_cumulative(c(0.1, 0.3))
survival_from_pd(c(0.1, 0.2))
cumulative_pd_from_conditional(c(0.1, 0.2))
component_ecl(0.02, 100000, 0.4, 0.05, 1)
cash_shortfall_ecl(list(c(1, 105)), list(c(2, 55.125)), 0.05)
modification_gain_loss(100, list(c(1, 99)), 0.1)
hedge_ineffectiveness(100, -90)
ead_profile(100, 50, 0.4, 0.1)
recovery_present_value(list(c(2, 121)), 0.1)
lgd_from_recoveries(100, list(c(1, 55)), 0.1)
provision_matrix_ecl(1000, 0.03)
weighted_ecl(c(base = 10, stress = 30), c(base = 0.75, stress = 0.25))
poci_allowance_change(20, 30)
liability_modification_test(100, 110)
coverage_ratio(20, 100)
benchmark_ecl(10)
}
\references{
International Accounting Standards Board (2014), IFRS 9,
paragraphs 5.4.1, 5.5.17, B3.3.6 and Appendix A (effective interest and
expected credit losses). See the independently maintained official source
index at \url{https://www.ifrs.org/issued-standards/list-of-standards/ifrs-9-financial-instruments/}.
}
\seealso{
\code{\link[=scenario_ecl]{scenario_ecl()}}, \code{\link[=interest_revenue]{interest_revenue()}}, \code{\link[=process_modification]{process_modification()}}
}
