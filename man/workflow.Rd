% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{workflow}
\alias{workflow}
\alias{run_dataset}
\alias{get_metrics}
\alias{get_table}
\alias{get_controls}
\alias{get_lineage}
\alias{export_results}
\title{Complete In-Memory Accounting Workflow and Result Accessors}
\usage{
run_dataset(dataset, policy = NULL)

get_metrics(result)

get_table(result, name)

get_controls(result)

get_lineage(result)

export_results(result, destination)
}
\arguments{
\item{dataset}{A named canonical 19-table list or a directory created by
\code{export_profile()} containing one JSON file per table.}

\item{policy}{Explicit versioned policy list. \code{NULL} selects the packaged
\code{EU_FINANCIAL_INSTRUMENTS_2026_V1} policy.}

\item{result}{An object returned by \code{run_dataset()}.}

\item{name}{Exact \code{book/sheet} result key or a unique sheet name.}

\item{destination}{A new directory path. Existing paths are rejected.}
}
\value{
\code{run_dataset()} returns an immutable-by-contract S3 object of class
\code{fae_analysis_result} containing \code{tables}, \code{metrics}, \code{lineage}, \code{status}
and \code{official=FALSE}. Accessors return deep copies. \code{export_results()}
invisibly returns the normalized destination after writing seven grouped
JSON files, \code{results.json} and a SHA-256 manifest.
}
\description{
Validate and calculate a complete canonical dataset using the same native R
functions exposed for granular analysis. Access named output tables, metrics,
controls and audit lineage, or export a read-only JSON result package.
}
\details{
A rejected input never yields a partially approved result. Run
control and policy identifiers/dates must agree. The workflow performs no
network access, journal posting or mutation of inputs. \code{APPROVED_REFERENCE}
means all embedded controls passed; it is not an institutional or official
accounting approval. Lineage includes deterministic input, policy, rule,
code and resource hashes and contract version \verb{1.1.0}.
}
\examples{
result <- run_dataset(load_reference_data("SMALL_SA_RETAIL_FIA"))
result$status
get_metrics(result)$opening_gross_exposure
length(get_table(result,"STAGING"))
all(vapply(get_controls(result), function(x) x$status == "PASS", logical(1)))
get_lineage(result)$contract_version
target <- tempfile("fae-results-")
export_results(result,target)
unlink(target,recursive=TRUE)
}
\references{
International Accounting Standards Board (2014), IFRS 9 and
IFRS 7. Official source indexes: \url{https://www.ifrs.org/issued-standards/}.
}
\seealso{
\link{reference_data}, \link{classification_decisions}, \link{risk_event_hedge},
\link{reporting_monitoring}
}
